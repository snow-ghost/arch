interface:
  display_name: "Architecture Guard"
  short_description: "Review code, compare versions, and map architecture"
  default_prompt: "Use $arch to review this change or compare two versions and report evidence-backed architecture deltas."
